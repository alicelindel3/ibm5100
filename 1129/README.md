## 書式
SELECT  
FROM  
WHERE  
GROUP BY  
COUNT / SUM / AVG / MAX / MIN  
HAVING  
ORDER BY  
LIMIT

## 実行順序
FROM  
ON / JOIN  
WHERE  
GROUP BY  
COUNT / SUM / AVG / MAX / MIN  
HAVING   
SELECT / DISTINCT  
ORDER BY  
LIMIT
